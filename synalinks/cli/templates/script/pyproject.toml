[project]
name = "synalinks-script"
version = "0.1.0"
description = "A minimal Synalinks script project."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "synalinks",
    "python-dotenv",
]
