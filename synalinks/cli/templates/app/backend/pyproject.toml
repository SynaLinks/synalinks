[project]
name = "synalinks-app-backend"
version = "0.1.0"
description = "The Synalinks + FastAPI backend for the full-stack app template."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "synalinks",
    "python-dotenv",
    "fastapi[standard]",
    "uvicorn",
]
